{
  "name": "eagles-tacos-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('No build step required')\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.115.0"
  }
}
